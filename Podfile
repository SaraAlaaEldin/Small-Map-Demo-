# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DemoForMap' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DemoForMap
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  pod 'Firebase/Core'
  pod 'SwiftyJSON'
  pod 'Alamofire' ,'~> 4.7'

  target 'DemoForMapTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DemoForMapUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
